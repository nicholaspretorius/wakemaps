# Wakemaps

## Run the app (locally)

* `npm install`
* `npm start`
* Setup .env file for necessary environment variables.

## Tests

* `npm test`
* `./node_modules/.bin/react-scripts test --coverage` 